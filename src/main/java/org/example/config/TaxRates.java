package org.example.config;

import java.math.BigDecimal;

public class TaxRates {
    public static final BigDecimal BASIC_SALES_TAX_RATE = BigDecimal.valueOf(0.10);
    public static final BigDecimal IMPORT_DUTY_RATE = BigDecimal.valueOf(0.05);
}
